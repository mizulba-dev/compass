# Postgres connection string. See docker-compose.yml for local dev values.
DATABASE_URL=postgres://compass:compass@localhost:55432/compass?sslmode=disable

# HTTP port the server listens on.
PORT=8080

# Directory containing the built SPA (web/dist after `npm run build`).
STATIC_DIR=web/dist
